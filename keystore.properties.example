# Copy this file to keystore.properties after creating the permanent release key.
# Never commit keystore.properties or the keystore itself.
storeFile=/absolute/path/to/fold-count-release.jks
storePassword=replace-with-store-password
keyAlias=fold-count
keyPassword=replace-with-key-password
